# API — filled from backend stack output after deploy (api.base_url)
VITE_API_BASE_URL=

# Cognito — filled from backend stack output after deploy
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_REGION=
VITE_COGNITO_DOMAIN=

# OAuth redirect — derived from deployed frontend URL (auth.oauth.redirect_uri)
VITE_OAUTH_REDIRECT_URI=

# Optional: legacy Google GSI preview (demo mode when API/Cognito unset)
VITE_GOOGLE_CLIENT_ID=
